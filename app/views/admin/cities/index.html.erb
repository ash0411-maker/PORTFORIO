<div class = "container">
	<div class = "row">
		<%= render 'admin/cities/errors', city: @city %>
		<div class = "col-xs-2">
			<div class = "form">
				<h3>都市登録</h3>
				<%= form_for(@city, url: admin_cities_path) do |f| %>
					<div class = "form_field">
			            <%= f.label :都市名 %><br/>
			            <%= f.text_field :name, class: "admin-city-post" %>
		        	</div>
		        	<div class = "form_field">
			        	<%= f.submit  "追加", class: "btn btn-success admin-city-post" %>
			        </div>
				<% end %>
			</div>
		</div>
		<div class = "col-xs-6 col-xs-offset-1">
			<h3 class = "flash"><%=flash[:notice]%></h3>
			<h2>都市一覧</h2>
			<table class = "table">
				<thead>
					<th>都市名</th>
					<th></th>
					<th></th>
				</thead>
				<% @cities.each do |city| %>
					<tbody>
						<td><%= city.name %></td>
						<td><%= link_to "編集", edit_admin_city_path(city), class: "btn btn-primary" %></td>
						<td>
							<%= link_to "削除", admin_city_path(city), method: :delete,data: {confirm:'削除してもよろしいですか？'}, class: "btn btn-danger" %>
						</td>
					</tbody>
				<% end %>
			</table>
		</div>
	</div>
</div>